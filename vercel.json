{
  "version": 2,
  "builds": [
    {
      "src": "main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "main.py" }
  ],
  "installCommand": "python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt",
  "buildCommand": "python3 -m prisma fetch && python3 -m prisma generate && python3 -m prisma migrate deploy"
}
